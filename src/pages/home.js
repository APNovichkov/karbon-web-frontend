import React from 'react'

// Import components
import SearchHeader from "./../components/searchHeader";

const Home = (props) => {
    return (
        <div className="home-wrapper">
            <SearchHeader/>
        </div>
    )
}

export default Home;